# index
 >  
 
 具体规则如下：
 ```js
module.exports = Object.assign(
  {},
  color,
  font,
  functionRules,
  number,
  string,
  length,
  value,
  shorthand,
  property,
  declaration,
  block,
  selector,
  rule,
  media,
  atRule,
  comment,
  general
);
```