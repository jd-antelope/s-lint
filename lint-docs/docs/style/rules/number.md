# number
 ```
"use strict";

module.exports = {
  // 小数时，不需要前面的 0
  "number-leading-zero": ["never", {"message":"禁止小数前面的 0"}]
  // 待定
  // "number-max-precision": 8,
  // "number-no-trailing-zeros": true
};
```